# ─── SFTP Server (Windows) ──────────────────────
SFTP_HOST=35.178.204.42        # Windows server IP or hostname
SFTP_PORT=22                     # Default SSH/SFTP port
SFTP_USER=sftpuser               # SFTP username
SFTP_PASSWORD=Adeniyi123 # Use password OR key (not both)
# SFTP_KEY_PATH=/run/secrets/sftp_key   # Path to private key file (PEM)
# SFTP_KEY_PASSPHRASE=                  # Passphrase if key is encrypted
SFTP_REMOTE_DIR=/uploads

# ─── AppSail ────────────────────────────────────
PORT=9000
NODE_ENV=production